import React from "react";
import renderer from "react-test-renderer";

import ManageSessions from "../(tabs)/(session)/manage";

/**
 *!SECTION Mocking
 *	I should mock the definitions of my data somewhere, 
 *  since the data is relatively well defined at this point 
 */

describe("manage sessions page tests", () => {

	it("loads sessions if they exist", () => {

	});
});